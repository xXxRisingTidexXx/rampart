run:
  concurrency: 4
  timeout: 30s
linters-settings:
  funlen:
    lines: 70
linters:
  enable-all: true
  disable:
    - wsl
    - gochecknoglobals
    - goimports
    - goerr113
    - gochecknoinits
    - gofumpt
    - noctx
